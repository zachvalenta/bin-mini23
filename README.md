```sh
├── bin
│   └── bz       # wrapper for aider to interactively choose model and handle local/global input and history files
│   └── fr       # https://github.com/birchb1024/frangipanni
│   └── gco      # debug git config
│   └── hmm      # https://github.com/nadrad/h-m-m
│   └── ghc      # view Github contribution counts (total, yearly/rolling averages)
│   └── jb       # git status wrapper called after cd-ing into notes repos
│   └── qing     # safe rm
│   └── sysinfo  # snapshot CPU/mem/ports
│   └── vimv     # vim-like interactive file editing
│   └── zy       # interactively search my repos|topics + take action (clone, open in browser)
```